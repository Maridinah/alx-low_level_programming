#include<stdio.h>
/**
 * main - Entry point
 *
 * Description: 'Q5only use the putchar function'
 *
 * Return: Always 0(Success)
 */
int main(void)
{
int a = 0;
for (; a < 10 ; a++)
putchar('0' + a);
putchar('\n');
return (0);
}
